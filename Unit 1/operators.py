#def goodMorningMSg():
    #username = input("what is your name?")
    #print('good morning '+ username)

#goodMorningMSg()

#def compoundInterest():
    #investAmount = 100
    #investAmount *= .05
    #moneyGained = investAmount
    #print(moneyGained)


#compoundInterest()

#def totalCost(itemName, itemPrice):
    #tax = 0.10
    #totalTax = itemPrice * tax
    #totalPrice = totalTax + itemPrice
    #print(totalPrice)

#totalCost('artic Coat',150)
#"Atric coat total is $275.000"  
#"Snow boots total is $165.00"
#Googles total is 110.000"

#myNumbers = [10,8,3, 28,300,57]

#myNumbers.sort()

#print(myNumbers)   



#Objects are construct for storing data and fuctions together
# when creating an object we start with the class keyword
# This acts like our object maker/ our blueprint
#class CarMaker:
    #def_init_(self, color, seating, year, model):
    #self.name = name
    #self.color = color
    #self.seating = seating,
    #self.year = year
    #self.model = model

    #def printInfo(self):
        #print('here your car faqs')
        #print('name: '+ self.name)
        #print('year: '+ (self.year))
        #print('miles: '+ str(self.miles))



    #def windshieldwippers():
        #print('when raining turn on')
    
    #def airbag():
        #print('when driving a certain speed and a collision happens; open')


    #def turnsignals(up, down): 
        #if up:
            #print("turn left")
        #elif down:
            #print("turn right")
        #else:
            #print("dont turn signals on")   

    #def bluetooth(year):
        #if year > 2020:
            #print("you have bluetooth")
        #else:
            #print("no bluetooth okn this model")


#carOption1 = CarMaker('carolla', 'black', '2', '2024', 'carolla' 2000) #creating actual car

#print(carOption1)
#carOption1.printInfo()


class Phone:
    def_init_(self, color, storage, number, model)
    self.name = self
    self.name = color
    self.name = storage
    self.name = number
    self.name = model



class ttProfile:
   def_init__(self, ttFollowers, ttFollowing, videosWatched, FavVideos, saves, likecount):
    self.username = username 
    self.ttFollowers = ttFollowers
    self.ttFollowing = ttFollowing
    self.videosWatched = videosWatched

   def postVideo(self):
       video=input("upload video ...")
       if videoFormat == 'avi':
       elif videoFormat == 'mp4':
          print("do some thing")
        else:
          print("video format not supported")   